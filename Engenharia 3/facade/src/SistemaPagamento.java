public class SistemaPagamento {
    public void processarPagamento(String produto, int quantidade) {
        System.out.println("Processando o pagamento para " + quantidade + " unidade(s) de " + produto);
    }
}
